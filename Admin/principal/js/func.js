function eliminaru(url) {
    if (window.confirm('Desea eliminar este usuario')) {
        window.location = url;
    }
}

function eliminarc(url) {
    if (window.confirm('Desea eliminar este cliente')) {
        window.location = url;
    }
}

function eliminarp(url) {
    if (window.confirm('Desea eliminar este producto')) {
        window.location = url;
    }
}

function agregarc(url) {
    if (window.confirm('Desea agregar el cliente')) {
        window.location = url;
    }
}